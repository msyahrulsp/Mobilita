#include <stdio.h>

#include "point.h"

int main() {
    /* KAMUS */
    POINT point;
    /* ALGORITMA */
    point = P_makePOINT(1, 1);
    P_displayPOINT(point);
    
    // Note: Sisa Function dan Procedure tidak dites dalam driver karena tidak digunakan 
}