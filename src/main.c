#include <stdio.h>
#include <stdlib.h>

int main(){

    boolean running;
    // kapasitas tas int
    // ability (speed boost , return to sender) list statis boolean

    // main menu

    while (running){

        // input
            // move
                // waktu
            // pick_up
            // drop_off
            // map
            // to_do
            // in_progress
            // buy (gadget)
            // inventory (gadget)
            // help
            // save game (bonus)
            // return (bonus)
            // endgame -> running = false

    }

    return 0;
}
